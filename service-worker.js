// service-worker.js
// A minimal service worker. Since you don't need offline functionality,
// this can be very basic. It just needs to exist.
self.addEventListener('fetch', function(event) {
  // You can add caching logic here later if needed,
  // but for now, we'll just let fetches go through.
});